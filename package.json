{
  "name": "nutrient-examples",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.7",
    "husky": "^9.1.7",
    "lint-staged": "^15.4.1"
  },
  "scripts": {
    "install-dependencies": "npx ts-node ./scripts/install-dependencies.ts",
    "format": "biome format --write .",
    "lint": "biome lint --write .",
    "prepare": "husky",
    "e2e-tests": "pnpm run install-dependencies && npx ts-node ./scripts/e2e-tests.ts",
    "lint-staged": "lint-staged"
  },
  "lint-staged": {
    "*": ["biome format --write", "biome lint --write"]
  }
}
